To compile the project, enter in the 'src' directory and execute the 'make' command. You will obtain an executable 'main', you can run it writing './main'.
The program will ask for the TREXIO input filename, so you have to write it.
